#include <string>
#include <time.h>
#include "AntSimulator.h"

class BaseAnt
{
private:
    int AntSize = 1;
    int Antposition[2] = {0, 0};
    int AntDirection = 0;
    //    AntSimulator &the_connected_sim;

public:
};